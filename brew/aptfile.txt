# System-level packages installed via apt
# Usage: grep -v '^#' aptfile.txt | grep -v '^$' | xargs sudo apt-get install -y

# Build essentials
build-essential
cmake
pkg-config

# Core utilities
curl
wget
git
stow
unzip
zip
tar

# Shell
zsh

# Networking
nmap
telnet
tcptraceroute

# Development
shellcheck
shfmt
jq
yamllint
perl

# Python build dependencies (for asdf to compile CPython from source)
libssl-dev
zlib1g-dev
libbz2-dev
libreadline-dev
libsqlite3-dev
libncursesw5-dev
xz-utils
tk-dev
libxml2-dev
libxmlsec1-dev
libffi-dev
liblzma-dev
