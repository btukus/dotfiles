# List of plugins
set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'wfxr/tmux-power'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# Theme
# set -g @tmux_power_theme 'snow'
set -g @plugin 'arcticicestudio/nord-tmux'
set -g default-terminal "$TERM"
set -ag terminal-overrides ",$TERM:Tc"

################################################################################################
##                                        Configuration                                       ##
################################################################################################

# set first window to index 1 (not 0) to map more to the keyboard layout
set -g base-index 1
setw -g pane-base-index 1
set-option -g renumber-windows on
set -sg escape-time 0

# Tmux options
set-window-option -g mode-keys vi
set -g mouse on

# Copy options
set -g @yank_with_mouse on
# tmux-yank sends mouse selections to X11 PRIMARY by default; use CLIPBOARD so Ctrl+V pastes them
set -g @yank_selection_mouse 'clipboard'

# Forward clipboard over OSC 52 (works through SSH to Mac terminal)
set -s set-clipboard on
set -as terminal-features ',*:clipboard'

# Restore sessions
set -g @continuum-restore 'on'
set -g @resurrect-strategy-nvim 'session'
set -g @resurrect-capture-pane-contents 'on'
set-option -g focus-events on

setw -g mode-keys vi
unbind-key -T copy-mode-vi v
bind-key -T copy-mode-vi 'v' send -X begin-selection     # Begin selection in copy mode.
bind-key -T copy-mode-vi 'C-v' send -X rectangle-toggle  # Begin selection in copy mode.
bind-key -T copy-mode-vi 'y' send -X copy-selection      # Yank selection in copy mode.

bind-key -T copy-mode-vi M-h resize-pane -L 1
bind-key -T copy-mode-vi M-j resize-pane -D 1
bind-key -T copy-mode-vi M-k resize-pane -U 1
bind-key -T copy-mode-vi M-l resize-pane -R 1

set -g prefix M-f # prefix set to Alt-f
# Splits and new windows inherit the current pane's context:
# - if the pane is running ssh, reuse the same ssh command in the new pane
# - otherwise fall back to inheriting the pane's cwd
bind M-k run-shell '~/dotfiles/scripts/tmux-smart-open.sh split-h'
bind M-j run-shell '~/dotfiles/scripts/tmux-smart-open.sh split-v'
bind c   run-shell '~/dotfiles/scripts/tmux-smart-open.sh window'

# Reload tmux config in place.
bind r source-file ~/.config/tmux/tmux.conf \; display-message "tmux config reloaded"

# swap windows/pages
bind-key -n M-p swap-window -t -1\; select-window -t -1
bind-key -n M-n swap-window -t +1\; select-window -t +1

# break current pane into its own window
bind-key -n M-b break-pane

# Alt equivalents of the macOS Alacritty Cmd bindings (on Linux, GNOME owns most Super keys)
bind-key -n M-t run-shell '~/dotfiles/scripts/tmux-smart-open.sh window'
bind-key -n M-w kill-pane
bind-key -n M-1 select-window -t :1
bind-key -n M-2 select-window -t :2
bind-key -n M-3 select-window -t :3
bind-key -n M-4 select-window -t :4
bind-key -n M-5 select-window -t :5
bind-key -n M-6 select-window -t :6
bind-key -n M-7 select-window -t :7
bind-key -n M-8 select-window -t :8
bind-key -n M-9 select-window -t :9

bind -n M-s choose-tree -s -O name -ZsK '#{?#{e|<:#{line},9},#{e|+:1,#{line}},#{?#{e|<:#{line},35},M-#{a:#{e|+:97,#{e|-:#{line},9}}},}}' \; \

# Clear screen bound to Alt-l
bind -n M-i send-keys 'C-l'

bind-key -N "Toggle persistent popup shell" \
         -n M-j if-shell -F '#{==:#{session_name},app}' { detach-client } \
         { display-popup -w 95% -h 95% -E -d '#{pane_current_path}' \
         'tmux attach -t app || tmux new-session -s app -c "#{pane_current_path}"'
         }

bind-key -N "Toggle persistent popup shell" \
         -n M-k if-shell -F '#{==:#{session_name},k9s}' { detach-client } \
         { display-popup -w 95% -h 95% -E -d '#{pane_current_path}' \
         'tmux attach -t "k9s" || (tmux set-environment -g EDITOR nvim; tmux set-environment -g VISUAL nvim; tmux new-session -s "k9s" \; attach-session -t "k9s")'
         }

run '~/.tmux/plugins/tpm/tpm'
